interface:
  display_name: "Loop Execute"
  short_description: "Implement authorized work and verify it"
  default_prompt: "Use $loop-execute to implement the authorized work directly and verify it proportionately."

policy:
  allow_implicit_invocation: false
